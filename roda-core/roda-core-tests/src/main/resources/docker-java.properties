# Pin docker-java client API version so Testcontainers can talk to modern
# Docker daemons (Docker 29+ rejects the old default 1.32 handshake).
# 1.40 is within range of both CI's Docker (<=1.47) and local Docker 29 (>=1.40).
api.version=1.40
